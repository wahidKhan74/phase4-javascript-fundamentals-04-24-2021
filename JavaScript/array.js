let items = ["Apple",'Orange',10,20,true,null,{ name : "mango", quantity:20}];

console.log(items);
console.log(items[1]);

//push : add element at the end
items.push("Iphone");

// pop : remove element from last
items.pop();

// unshift : add element in start
items.unshift("Mr.");

// shoft : remove element from start.
items.shift();
